# 바닷가 화장실 — 앱인토스 미니앱

여름 시즌 웹뷰 방식 미니앱. 기획 확정본은 [docs/기획안.md](docs/기획안.md) — **작업 전 필독. 기획안과 다른 동작을 임의로 추가하지 말 것.**

한 줄 컨셉: "물놀이 가서 여는 지도" — 해수욕장·계곡의 화장실/샤워실 위치·상태를 GPS 인증 제보로 쌓는다.

## 아키텍처 (땡모반 지도 구조 승계)

- **웹뷰 방식** (Granite RN 아님) — 지도 JS SDK가 웹에서만 동작하기 때문. ait-factory 규칙(플레이스홀더 ID 체계, M3 교체)은 승계.
- Vite + TypeScript, 프레임워크 없음. 탭 2개(지도/포인트, 하단 플로팅 알약) + 스팟 바텀시트 + 제보 시트가 화면 전부.
- `src/config.ts`에 운영 전환 시 교체할 값 집중: 카카오 JS 키, 광고 지면 ID(`ait.v2.live.__REPLACE_AT_M3__`), 보상 확률표, 일일 한도(10), GPS 반경(200m), `DEV_BYPASS_GPS`.
- **카카오맵 SDK 로드 실패 시 목업 지도 폴백** 자동 동작 — 개발은 키 없이 가능. 로컬 개발 중 실제 지도로 확인하려면 `.env.local`(gitignore 대상)에 `VITE_KAKAO_JS_KEY=발급받은키` 추가. M3(운영 전환) 시점엔 `config.ts`의 플레이스홀더 상수를 실키로 교체. 카카오 콘솔 Web 플랫폼에 `http://localhost:5199` / `*.apps.tossmini.com` / `*.private-apps.tossmini.com` 도메인 등록 필수(도메인 미등록이 무한 로딩의 추정 원인, techchat 3859 — `loadKakaoSdk()`가 script 로드와 `kakao.maps.load()` 콜백 양쪽에 개별 타임아웃+콘솔 진단을 둬서 원인을 구분해준다). 실패 시 네이버지도 폴백 검토.
- 네이티브 의존(위치/저장소)은 `src/bridge.ts`, 광고는 `src/ads.ts`에 격리. M3에서 `@apps-in-toss/web-framework` 연동 시 이 두 파일만 교체.
- 데이터는 `src/data.generated.ts` (커밋됨, `npm run build:data`로 재생성). `src/data.ts`는 타입·헬퍼만 두고 생성 파일을 재수출한다.

## 데이터 배치 (`npm run build:data`)

`scripts/build-data.mjs` 5단계. 각 단계 결과는 `data-raw/cache/`에 캐시돼 중단 후 재개된다.

| 단계 | 내용 |
|---|---|
| 1 | 카카오 키워드 검색으로 전국 해수욕장/계곡 수집 (좌표 포함) |
| 2 | 스팟이 있는 시군구의 화장실만 추림 (5만건 전수 지오코딩 회피) |
| 3 | 추린 화장실 주소 → 좌표 (표준데이터는 2025.2부터 좌표 미제공) |
| 4 | 스팟별 샤워장/화장실 POI 검색 |
| 5 | 반경 1.5km 내 최근접 스팟에 배정 → `src/data.generated.ts` |

현재 산출: **스팟 547곳(해수욕장 380/계곡 167) · 시설 1,729건**.

⚠️ **'해변' 표기를 빠뜨리지 말 것.** 동해안 상당수가 '○○해변'으로 불려서, '해수욕장'으로 끝나는 이름만 받으면 지역이 통째로 빠진다(강릉시 19곳 중 3곳만 잡혔던 버그). 수집기는 두 표기를 모두 검색하고 `dedupeNearby()`로 병합한다 — 같은 곳의 다른 표기는 600m, 같은 이름(`몽돌` 등 전국 다발)은 1.5km 조건에서만 병합.

### 배치에서 실제로 부딪힌 것들

- **화장실 표준데이터 원본은 로그인 없이 받을 수 있다.** `https://file.localdata.go.kr/file/download/public_restroom_info/info` — 단 브라우저 UA + `X-XSRF-TOKEN`(같은 사이트 `/file/public_restroom_info/info` 페이지의 `_csrf` meta) + GET이어야 한다. `data-raw/toilets.csv`(EUC-KR→UTF-8 변환본, 53,547행)는 용량 때문에 gitignore.
- **data.go.kr의 파일 다운로드(해수욕장 개폐장일정 등)는 로그인이 필요해 자동화 불가.** 그래서 해수욕장 목록은 카카오 키워드 검색으로 대체했다(251곳, 공식 264곳과 근접).
- **스팟 이름은 반드시 '해수욕장/계곡'으로 끝나야 한다.** 단순 포함 검사로는 "보스턴수제버거 동막해수욕장", "CU 강화동막해수욕장점", "송정해수욕장 1호 공중화장실"이 스팟으로 잡힌다.
- **표준데이터의 '화장실명'은 건물명인 경우가 많다**("해변주유소", "중1동 주민센터"). `toiletLabel()`이 접미사를 붙인다.
- **샤워장은 자동 수집이 불가능하다 (3경로 검증 완료).** 카카오 POI 0건(해운대·대천·속초), 지자체 공공데이터는 개수만·좌표 없음, 해수부 바다여행 포털도 '샤워장 있음' 불리언만. `data-raw/showers-manual.json`으로 수작업 시딩한다.
  - `showers[]` — **좌표를 아는 경우만.** 지자체 공지의 '공용화장실 N호 앞' 서술을 이미 수집된 화장실 좌표에 매핑한다. **좌표를 지어내지 말 것.**
  - `spotShowerNotes[]` — 샤워장 존재·요금은 확인됐으나 좌표를 못 구한 경우. 스팟 상세에 한 줄로 노출(`Spot.showerNote`)하고 핀은 찍지 않는다. 잘못된 핀은 이 앱이 없애려는 문제 그 자체라, 모르는 위치는 모른다고 표시한다.
  - 두 배열 모두 `source`(근거 URL) 필수. 근거 없는 값 금지.
- 화장실 주소가 지저분해(시도 누락/공백 없음) 위치 파싱 대신 **시군구 문자열 포함** 검사로 후보를 추린다. 지오코딩 성공률 79%.
- **'해변' 표기를 놓치면 동해안이 통째로 빠진다.** 강릉시는 해변 19곳 중 3곳만 잡혀 있었다 — 수집기가 '해수욕장'으로 끝나는 이름만 받았기 때문. 두 표기를 모두 수집하고 `dedupeNearby()`로 병합한다.
- **샤워장은 화장실에 밀려 사라지기 쉽다.** 샤워장은 대개 화장실 바로 옆이라 ① 중복 제거는 반드시 같은 type끼리만(`isDuplicate`), ② 스팟당 개수 제한에서 샤워장 우선 배치. 둘 다 실제로 대천 공영샤워장을 잃고 나서 고쳤다.
- **카카오 키워드 검색은 편차가 크다.** '대천해수욕장 샤워장' 0건 / '충남 해수욕장 샤워장' 1건. 스팟별(`stageSpotPois`)과 지역별(`stageRegionShowers`) 스윕을 둘 다 돌린다.
- **TourAPI는 샤워/부대시설 정보가 없다** (contentTypeId=12의 detailIntro2/detailInfo2 확인). 해수욕장 시설 정보원으로 쓸 수 없다. 참고: 파라미터 이름은 `ServiceKey`(대문자 S)여야 하고 `serviceKey`는 Unauthorized.

### 해양수산부 파일 데이터 (사람이 내려받아 `data-raw/`에 배치)

data.go.kr 파일 다운로드는 로그인이 필요해 자동화 불가. 없으면 배치가 조용히 건너뛴다.

| 파일 | 용도 |
|---|---|
| `beach-schedule-2024.xlsx` | 개장/폐장일 280곳 → `openStart`/`openEnd`/`notOpening`. **개장 배지의 근거** |
| `beach-visitors.csv` | 연도별 이용객 260곳 → `note`("이용객 전국 N위") + 샤워 시딩 우선순위 출력 |

- 파서는 `scripts/lib/beachmeta.mjs` (의존성 없이 xlsx를 직접 푼다).
- 이름 표기가 제각각이라(`경포대`/`경포해변`/`경포해수욕장`) `normKey()`로 접미사·괄호·공백을 떼고 매칭한다. 현재 매칭률 약 50% — 카카오가 공식 264곳보다 많이 찾아서 나머지는 비지정 해변.
- **개장 배지는 단정하지 않는다.** 근거가 2024년 일정이라 연도별로 며칠씩 다르다 → '개장 시즌/개장 예정/폐장'으로 표기하고 기간 뒤에 '(2024년 기준)'을 붙인다. 근거가 없는 스팟은 배지를 아예 달지 않는다(예전엔 전부 '개장중'으로 표시해 사실과 달랐다).

## 불변 규칙

1. **광고 시점 2단계 분리** (justin 확정 2026-07-26). 지면 3종은 `config.ts`에만 둔다.
   - **제보 완료 → 전면형**(`interstitial`). 이때는 **앱 내 포인트로 적립**만 된다.
   - **포인트 탭 '전환' → 보상형**(`rewarded`). **시청 완료 시에만** 토스포인트로 전환된다.
   - 배너(`banner`)는 상시 하단.
   - 두 시점이 분리돼 있어 풀스크린 광고가 연달아 뜨지 않는다. 한 흐름에 둘 다 띄우지 말 것.
   - 포인트 원장: `totalEarned()`(적립 누적) − `totalConverted()`(전환 누적) = `pendingPoints()`(전환 가능). 실제 토스포인트 지급 API 연동 지점은 `convertPending()`.
2. **보상 확률표·한도는 `config.ts`에서만 변경**: 100원 99.4% / 500원 0.4% / 1,000원 0.2% (EV≈103원), 1일 10회. ⚠️ 광고 회당 수익 < EV면 역마진 — M3 전 금송아지 실측 eCPM 대조 필수.
3. **GPS 200m 인증 + 같은 시설 1인 1일 1회** 어뷰징 방어를 약화시키지 말 것. `DEV_BYPASS_GPS`는 M3에서 반드시 false.
4. **외부 이동 금지**: 외부 브라우저/앱 스킴으로 사용자를 내보내는 코드 금지 (앱인토스 정책).
5. **이모지 UI 금지** — 아이콘은 `src/ui.ts`의 TDS 톤 SVG만 사용 (justin 결정, 2026-07-26).
6. 리뷰는 텍스트 없음 — 별점·청결도·요금·온수(샤워실만) 4항목 고정. 항목 추가는 기획 변경(justin 결정) 사항.

## 명령

- `npm run dev` — 개발 서버 (5199 포트 고정 — 5173은 다른 프로젝트가 사용 중)
- `npm run build` — 타입체크 + 빌드
- `npm run deploy` — GitHub Pages 재배포 → https://justincho-crypto.github.io/beach-toilet/

## 배포 URL (카카오 도메인 등록용)

- 저장소: https://github.com/JustinCho-Crypto/beach-toilet (public, `gh-pages` 브랜치 서빙)
- 공개 URL: **https://justincho-crypto.github.io/beach-toilet/** — 2026-07-26 실 카카오맵 렌더링 확인 완료
- 이 URL은 **카카오 도메인 등록 + 실 지도 동작 확인 목적**이다. 앱인토스 실제 출시는 별개(`BASE=/` 빌드 → `.ait` 번들 → 콘솔 업로드).
- 카카오 JS 키는 도메인 화이트리스트로 보호되는 **공개 키**라 번들에 포함돼도 정상. 단 등록 도메인은 필요한 것만 유지할 것.

### 카카오맵 설정 — 실제로 막혔던 지점 (재현 방지용 기록)

1. **제품 활성화가 먼저다.** 키가 맞아도 `제품 설정 → 카카오맵 → 활성화 설정 ON`이 안 돼 있으면 SDK가 403 `NotAuthorizedError: disabled OPEN_MAP_AND_LOCAL service`를 반환한다. 진단은 `curl "https://dapi.kakao.com/v2/maps/sdk.js?appkey=<키>&autoload=false"` — 200이면 정상, 403이면 이 문제.
2. **JS 키와 도메인 등록 위치가 개편됐다.** 예전 `앱 키` / `플랫폼 > Web 사이트 도메인`이 아니라, 지금은 `앱 설정 → 앱 → 플랫폼 키 → JavaScript 키`에서 키를 확인하고 같은 화면의 **JavaScript SDK 도메인**에 도메인을 등록한다.
3. 현재 등록된 도메인은 `https://justincho-crypto.github.io` **하나뿐**이라, **localhost에서는 실 지도가 안 뜨고 목업 지도로 폴백된다** (의도된 동작). 로컬에서 실 지도를 봐야 하면 `http://localhost:5199`를 추가 등록할 것.

### GitHub Pages 배포 주의

- `scripts/deploy-pages.sh`는 **worktree로 gh-pages 히스토리를 이어서** 커밋한다. 예전처럼 `git init` + force push를 하면 **GitHub이 Pages 사이트를 unpublish 해버린다** (실제 발생).
- 무료 플랜은 **private 저장소에 Pages를 지원하지 않는다.** 저장소가 private으로 되돌아가면 Pages가 즉시 죽는다.
- `docs/기획안.md`는 사업 전략 문서라 **공개 저장소에서 제외**(gitignore)되어 있다. 로컬에만 존재.
