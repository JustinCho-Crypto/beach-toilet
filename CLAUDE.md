# 바닷가 화장실 — 앱인토스(Apps in Toss) 미니앱

**무엇을 만드는가**: 해수욕장·계곡의 **화장실/샤워실 위치와 상태**를 지도에 띄우고, 현장 방문자의 GPS 인증 제보로 정보를 쌓는 토스 미니앱.
한 줄 컨셉은 **"물놀이 가서 여는 지도"**. 여름 시즌(7~8월) 한정 트래픽을 노린다.

기획 확정본은 `docs/기획안.md` (로컬 전용, 공개 저장소 제외) — **작업 전 필독. 기획안과 다른 동작을 임의로 추가하지 말 것.**

---

## 1. 이건 토스 미니앱이다 — 작업 원칙

일반 웹앱처럼 만들면 안 된다. 아래 셋은 매 작업에서 지켜야 한다.

### 1-1. 토스 공식 문서와 개발자 커뮤니티를 근거로 삼는다

추측으로 구현하지 말고 아래를 먼저 확인한다. 특히 **정책·정산·심사** 관련은 반드시 원문 확인.

- 개발자센터: https://developers-apps-in-toss.toss.im
  - 앱 정보 검토 가이드라인 `/prepare/checklist.html`
  - 미니앱 가이드라인 `/checklist/miniapp-service.html`
  - 브랜딩(로고 규격) `/design/miniapp-branding-guide.html`
  - 인앱 광고 `/ads/intro.html` · 프로모션(포인트 지급) `/promotion/intro.html` · 공유 리워드 `/reward/intro.html`
  - 출시 절차 `/development/deploy.html`
- **개발자 커뮤니티(techchat)**: https://techchat-apps-in-toss.toss.im — 공식 문서에 없는 실전 제약이 여기 답변에 있다. 실제로 이 프로젝트에서 인용한 스레드:
  - 인앱 지도 임베드 정책 허용 확인 (`/t/app-1/4125`)
  - 카카오맵 JS SDK 토스 웹뷰 무한 로딩 (`/t/js-sdk-webview/3859`) — 도메인 미등록이 원인으로 추정
  - 외부 앱/브라우저 이동 정책 (`/t/webview/3914`)
- 막히면 커뮤니티에 질문을 올리는 것도 정식 경로다. 정책 판단을 임의로 내리지 말 것.

### 1-2. 토스 자산(SDK·에셋)을 적극 활용한다

바퀴를 새로 만들지 말고 토스가 제공하는 것을 쓴다.

- **`@apps-in-toss/web-framework`** — 웹뷰 미니앱용. 위치·저장소·광고·공유가 여기 있다. 현재 `src/bridge.ts`·`src/ads.ts`가 브라우저 API로 임시 구현돼 있고, **M3에서 이 두 파일만 교체**하면 되도록 격리해 뒀다.
- **저장소는 반드시 공식 `Storage`** — 커뮤니티 AsyncStorage는 운영 토스에서 데이터가 유실된다(ait-factory 하드룰).
- **광고는 인앱 광고 2.0 API** (`loadFullScreenAd`/`showFullScreenAd`).
- **토스 3D 이모지 CDN**: `https://static.toss.im/3d-emojis/u{유니코드}.png` — 아이콘을 새로 그리기 전에 여기 있는지 먼저 본다.
- TDS(토스 디자인 시스템) 컴포넌트/톤을 따른다. 단 **일반 이모지 문자를 UI에 쓰지 말 것** (justin 결정) — 아이콘은 `src/ui.ts`의 TDS 톤 SVG를 쓴다.

### 1-3. 화면은 `docs/ui-patterns.md`를 먼저 보고 만든다

정식 출시된 앱(geumsongaji)에서 실제 검증된 패턴 모음. **새 화면을 설계하기 전에 반드시 읽고, 맞는 패턴이 있으면 그대로 재사용한다.** 특히:

- 색상/배경 톤, 카드·pill 규격
- 온보딩은 팝업이 아니라 **풀스크린 스텝 페이지**
- 광고 지면 3중 방어(지연 마운트 + 에러 바운더리 + onNoFill 폴백)
- 광고 잠금 버튼 문구는 예외 없이 **"광고 보고 ○○하기"**
- 보상 연출은 고정 텍스트가 아니라 **떠올랐다 사라지는 플로팅 아이템**
- 문구 톤(짧은 구어체, 실패도 다음 행동 안내로 끝냄)
- TDS 함정 워크어라운드(Badge 절대배치 어긋남, FixedBottomCTA `gradient={null}` 등)

---

## 2. 아키텍처

**웹뷰 방식** (Granite RN 아님) — 지도 JS SDK가 웹에서만 동작하기 때문. 선행 사례는 `../ttaengmoban map`. ait-factory의 규칙(플레이스홀더 ID 체계, M3에서 실 ID 교체)은 승계한다.

- Vite + TypeScript, 프레임워크 없음.
- 화면 전부: **탭 2개(지도 / 포인트, 하단 플로팅 알약)** + 스팟 바텀시트 + 제보 시트.
- `src/config.ts` — 운영 전환 시 교체할 값을 여기에만 둔다 (카카오 JS 키, 광고 지면 ID, 보상 확률표, 일일 한도, GPS 반경, `DEV_BYPASS_GPS`).
- `src/bridge.ts`(위치·저장소) / `src/ads.ts`(광고) — 네이티브 의존 격리. M3 교체 지점.
- `src/data.generated.ts` — 배치 산출물(커밋됨). `src/data.ts`는 타입·헬퍼만 두고 재수출.
- **카카오맵 SDK 로드 실패 시 목업 지도로 자동 폴백** → 키 없이도 개발 가능.

### 명령

| 명령 | 내용 |
|---|---|
| `npm run dev` | 개발 서버. **5199 포트 고정** (5173은 다른 프로젝트 사용 중) |
| `npm run build` | 타입체크 + 빌드 |
| `npm run build:data` | 공공데이터 배치 → `src/data.generated.ts` |
| `npm run deploy` | GitHub Pages 재배포 |

### 배포

**두 갈래가 있고 목적이 다르다. 혼동하지 말 것.**

1. **GitHub Pages** (현재 동작) — https://justincho-crypto.github.io/beach-toilet/
   저장소: https://github.com/JustinCho-Crypto/beach-toilet (public, `gh-pages` 브랜치)
   목적은 **카카오 도메인 등록 + 실 지도 동작 확인**이다. 앱인토스 출시와는 무관.
2. **앱인토스 배포** (미연결) — `~/.ait/credentials`에 mTLS 인증 정보가 준비돼 있어 **자동 배포가 가능한 상태**다. 단 `ait` CLI가 이 프로젝트 의존성에 아직 없다 (ait-factory 템플릿은 `ait deploy`를 쓴다). 출시 단계에서 CLI를 붙이고 `BASE=/` 빌드 → 배포로 연결할 것.

---

## 3. 불변 규칙

1. **광고 시점 2단계 분리** (justin 확정). 지면 ID 3종은 `config.ts`에만.
   - **제보 완료 → 전면형**(`interstitial`). 이때는 **앱 내 포인트 적립만**.
   - **포인트 탭 '전환' → 보상형**(`rewarded`). **시청 완료 콜백에서만** 토스포인트로 전환.
   - 배너(`banner`)는 상시 하단. **한 흐름에 풀스크린 광고를 연달아 띄우지 말 것** (앱인토스 '과도한 광고 노출' 가이드).
   - 포인트 원장: `totalEarned()` − `totalConverted()` = `pendingPoints()`. 실지급 API 연동 지점은 `convertPending()`.
2. **보상 확률표·한도는 `config.ts`에서만**: 100원 99.4% / 500원 0.4% / 1,000원 0.2% (EV≈103원), 1일 10회.
   ⚠️ **광고 회당 수익 < EV면 역마진.** M3 전 금송아지 실측 eCPM 대조 필수.
3. **어뷰징 방어를 약화시키지 말 것**: GPS 200m 인증 + 같은 시설 1인 1일 1회. `DEV_BYPASS_GPS`는 M3에서 반드시 `false`.
4. **외부 이동 금지** — 외부 브라우저/앱 스킴으로 유저를 내보내는 코드 금지 (앱인토스 정책).
5. **이모지 UI 금지** — `src/ui.ts`의 SVG만.
6. **제보 항목 고정** — 별점 · 청결도 · 요금 + 샤워실만 온수. 텍스트 리뷰·사진 없음. 항목 추가는 기획 변경(justin 결정).
7. **없는 데이터를 지어내지 말 것.** 좌표를 모르면 핀을 찍지 않고 문구로 알린다(§4-3). 잘못된 위치 정보는 이 앱이 없애려는 문제 그 자체다.

---

## 4. 데이터 배치 — 진행 중인 작업

`npm run build:data` (`scripts/build-data.mjs`). 각 단계는 `data-raw/cache/`에 캐시돼 중단 후 재개된다.
**캐시는 API 호출 결과라 커밋해 보존한다** (재수집 = 카카오 쿼터 재소모).

| 단계 | 내용 |
|---|---|
| 1 | 카카오 키워드 검색으로 전국 해수욕장/계곡 수집 (좌표 포함) |
| 2 | 스팟이 있는 시군구의 화장실만 추림 (5만건 전수 지오코딩 회피) |
| 3 | 추린 화장실 주소 → 좌표 (표준데이터는 2025.2부터 좌표 미제공) |
| 4 | 스팟별 + 지역별 샤워장/화장실 POI 검색 (2중 스윕) |
| 5 | 반경 1.5km 내 최근접 스팟에 배정 → `src/data.generated.ts` |

**현재 산출: 스팟 548곳(해수욕장 381 / 계곡 167) · 시설 1,740건 · 샤워 정보 30여 곳**

**API 예산 가드**: `scripts/lib/kakao.mjs`에 하드 상한(기본 3,000회, `KAKAO_MAX_CALLS`로 조정). 카카오 무료 한도는 일 10만인데 초기에 3만 회를 태운 이력이 있어 넣었다. 상한 도달 시 예외로 중단되고, 캐시가 있으니 재개하면 이어진다.

### 4-1. 데이터 출처와 접근 방법

| 출처 | 상태 |
|---|---|
| 전국공중화장실표준데이터 (53,547행) | **로그인 없이 자동 다운로드 가능**. `https://file.localdata.go.kr/file/download/public_restroom_info/info` — 브라우저 UA + `X-XSRF-TOKEN`(같은 사이트 `/file/public_restroom_info/info`의 `_csrf` meta) + **GET**. `data-raw/toilets.csv`는 용량 때문에 gitignore |
| 카카오 로컬 API | 스팟·샤워장·화장실 POI + 지오코딩. **JS 키로 호출 가능** — `Authorization: KakaoAK <JS키>` + `KA: sdk/1.0 os/javascript origin/<등록도메인>` 헤더 |
| 해수욕장 개폐장일정 `beach-schedule-2024.xlsx` | data.go.kr 파일 다운로드는 **로그인 필요 → 자동화 불가**. 사람이 내려받아 `data-raw/`에 둔다. 없으면 배치가 조용히 건너뜀 |
| 해수욕장 이용객 현황 `beach-visitors.csv` | 위와 동일. 시딩 우선순위 + "이용객 전국 N위" 표기 |
| TourAPI (한국관광공사) | **부대시설 정보 없음** — 샤워장 정보원으로 쓸 수 없다 (검증 완료). 참고: 파라미터는 `ServiceKey`(대문자 S), 소문자는 Unauthorized |
| 행안부 물놀이관리지역 | **도입 보류.** WMS(지도 이미지)만 제공해 개별 지점 등급을 데이터로 못 받고, 구 데이터 API는 폐지. 계곡 위험등급 기능 포기 결정 |

### 4-2. 지금 하고 있는 일 — 샤워장 정보 수집

**샤워장은 자동 수집이 불가능하다** (3경로 검증). 카카오 POI 0건(해운대·대천·속초 실측), 지자체 공공데이터는 개수만 있고 좌표 없음, 해수부 포털도 불리언만. → `data-raw/showers-manual.json`에 수작업 시딩한다.

두 배열로 나뉘며 **둘 다 `source`(근거 URL) 필수**:

- **`showers[]`** — **좌표를 아는 경우만.** 지자체 공지의 "공용화장실 N호 앞" 같은 서술을 이미 수집된 화장실 좌표에 매핑한다. 좌표를 추정해 넣지 말 것.
- **`spotShowerNotes[]`** — 존재·요금은 확인됐으나 좌표를 못 구한 경우. 스팟 상세에 한 줄로 노출(`Spot.showerNote`), 핀은 안 찍는다.

**수확 패턴(경험값)**: 해수욕장 한 곳당 검색 1~2회, 성공률 절반. 대부분 핀이 아니라 안내 문구로 들어간다. 지자체가 **요금은 공지하지만 위치는 안 알려주는** 게 일반적이다(속초: "2개소, 실내 1,000원 온수 / 야외 500원"인데 위치 없음).

**효율적인 방법**: 지자체 관광포털이 해변별 상세 페이지를 **URL 패턴으로 제공하면 스크립트로 일괄 수집**이 가능하다. 강릉시가 그랬다(`gn.go.kr/tour/sub03_02_NN.do` → 19곳을 HTTP만으로, API 호출 0회). 반면 양양·태안은 JS 렌더링/URL 추측 불가로 실패했다.

**남은 우선순위**는 배치 실행 시 이용객 순위로 자동 출력된다. 현재 최상단:
광안리(3위)·송도(4위)·다대포(7위) — 부산 3곳은 두 번 시도했으나 요금 고시 없음 / 삼척(12)·아야진(13)·함덕(15)·꽃지(16)·낙산(18)·춘장대(19)·송지호(20)

### 4-3. 배치 함정 (재발 방지 — 전부 실제로 데이터를 잃고 고친 것)

- **'해변' 표기를 빠뜨리면 동해안이 통째로 빠진다.** 강릉 19곳 중 3곳만 잡혔다. 두 표기를 모두 검색하고 `dedupeNearby()`로 병합 — 같은 곳의 다른 표기는 600m, 같은 이름(`몽돌` 등 전국 다발)은 1.5km 조건에서만.
- **스팟 이름은 해당 단어로 '끝나야' 한다.** 포함 검사로는 "보스턴수제버거 동막해수욕장", "CU 강화동막해수욕장점"이 스팟이 된다.
- **샤워장은 화장실에 밀려 사라진다.** 샤워장은 대개 화장실 바로 옆이라 ① 중복 제거는 **같은 type끼리만**(`isDuplicate`), ② 스팟당 개수 제한에서 **샤워장 우선 배치**. 둘 다 대천 공영샤워장을 잃고 나서 고쳤다.
- **카카오 키워드 검색은 편차가 크다.** '대천해수욕장 샤워장' 0건 / '충남 해수욕장 샤워장' 1건 → 스팟별(`stageSpotPois`) + 지역별(`stageRegionShowers`) 2중 스윕.
- **표준데이터의 '화장실명'은 건물명인 경우가 많다**("해변주유소", "중1동 주민센터") → `toiletLabel()`이 접미사를 붙인다.
- 화장실 주소가 지저분해(시도 누락/공백 없음) 위치 파싱 대신 **시군구 문자열 포함** 검사로 후보를 추린다. 지오코딩 성공률 79%.
- 이름 표기가 제각각이라(`경포대`/`경포해변`/`경포해수욕장`) `normKey()`로 접미사·괄호·공백을 떼고 매칭. 매칭률 약 50%(카카오가 공식 264곳보다 많이 찾아서 나머지는 비지정 해변).
- **개장 배지는 단정하지 않는다.** 근거가 2024년 일정이라 → '개장 시즌/개장 예정/폐장' + '(2024년 기준)' 병기. 근거 없으면 배지 미표시. 전엔 전부 '개장중'이라 사실과 달랐다.

### 4-4. 카카오맵 설정 함정

1. **제품 활성화가 먼저다.** 키가 맞아도 `제품 설정 → 카카오맵 → 활성화 ON`이 없으면 403 `disabled OPEN_MAP_AND_LOCAL service`. 진단: `curl "https://dapi.kakao.com/v2/maps/sdk.js?appkey=<키>&autoload=false"` → 200이면 정상.
2. **콘솔 메뉴가 개편됐다.** `앱 설정 → 앱 → 플랫폼 키 → JavaScript 키`에서 키를 보고, 같은 화면의 **JavaScript SDK 도메인**에 도메인을 등록한다(예전 '앱 키' / '플랫폼 > Web' 아님).
3. 현재 등록 도메인은 `https://justincho-crypto.github.io` **하나뿐** → localhost에서는 목업 지도로 폴백된다(의도된 동작). 로컬에서 실 지도를 보려면 `http://localhost:5199` 추가 등록.
4. JS 키는 도메인 화이트리스트로 보호되는 **공개 키**라 번들에 포함돼도 정상. 로컬 개발은 `.env.local`의 `VITE_KAKAO_JS_KEY`.

---

## 5. 다음 할 일

1. **샤워장 시딩 계속** (§4-2) — 이용객 상위권부터.
2. **제보 서버 연동** — 현재 제보가 `localStorage`에만 쌓여 **유저 간 공유가 안 된다**. 코어 루프가 실제로 작동하려면 필요. 출시 관점에서 가장 급한 항목.
3. **M3 전환** — 광고/위치/저장소를 `@apps-in-toss/web-framework`로 교체, `DEV_BYPASS_GPS=false`, 실 eCPM으로 역마진 검증.
4. **앱인토스 배포 경로 연결** — `ait` CLI 붙이기.
