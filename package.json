{
  "name": "beach-toilet",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "deploy": "bash scripts/deploy-pages.sh",
    "build:data": "node scripts/build-data.mjs",
    "ait:test": "VITE_AIT_ENV=test BASE=/ ait build && mkdir -p release && cp beachtoilet.ait release/beachtoilet-test.ait",
    "ait:live": "VITE_AIT_ENV=live BASE=/ ait build && mkdir -p release && cp beachtoilet.ait release/beachtoilet-live.ait",
    "ait:verify": "node scripts/verify-ait.mjs"
  },
  "dependencies": {
    "@apps-in-toss/web-framework": "2.10.7",
    "@supabase/supabase-js": "^2.45.4"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.8"
  }
}
