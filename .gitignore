node_modules
dist
# 빌드 산출물 — 매 빌드마다 deploymentId가 바뀌어 diff만 커진다. 필요하면 재빌드할 것.
*.ait
release/
# ait build가 granite.config.ts에서 생성하는 파일 (원본은 granite.config.ts)
.granite/
.env
.env.local
.env.*.local
.gh-pages-worktree
docs/기획안.md
data-raw/toilets.csv
# 캐시는 API 호출 결과라 커밋해 보존한다 (재수집 = 카카오 쿼터 재소모).
# 단 원본 CSV와 중간 후보 목록은 재생성이 공짜라 제외.
data-raw/cache/2-toilet-candidates.json
